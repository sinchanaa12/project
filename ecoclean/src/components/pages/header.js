import React from 'react';
import './header'; // Import a CSS file for your header styles

const Header = () => {
  return (
    <header>
      
      <h1 id="infrastructure-heading">
        Our Infrastructure Commitment
      </h1>
    </header>
  );
};

export default Header;